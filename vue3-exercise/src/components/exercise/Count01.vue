<script setup>
import {ref,inject} from "vue";
const message = inject("$message")//使用message的方法一
const count = ref(0)
const increment = () => {
    if(count.value < 99){
        count.value += 1
    }else{
        message.warn("最大是99")
    }
}
const decrement = () => {
    if(count.value > 0){
        count.value -= 1
    }else{
        message.warn("已经减到0了，不能再减了")
    }
}
</script>

<template>
    <div>计步器01：{{count}}</div>
    <a-button @click = "increment">+</a-button>
    <a-button @click = "decrement">-</a-button>
</template>

